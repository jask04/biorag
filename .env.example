# Copy to .env and fill in. .env is gitignored; never commit real values.

# Google AI Studio key for Gemini (needed from Day 9; reuse askrepo's key).
# GOOGLE_API_KEY=

# Qdrant Cloud cluster (needed from Day 4; free 1GB tier).
# QDRANT_URL=
# QDRANT_API_KEY=
